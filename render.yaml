services:
  - type: web
    name: halal-unlimited-bot
    runtime: node
    plan: free
    buildCommand: npm install && mkdir -p data/trades
    startCommand: node server.js
    healthCheckPath: /api/health
